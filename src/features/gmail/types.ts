/** Wire types of the `gmail` Edge Function — one definition, shared with the function. */
export type { Address, GmailStatus, InboxAccount, InboxThread, ThreadInsight } from '../../../supabase/functions/_shared/gmail-contract';
